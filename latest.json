{
    "version": "1.3.4",
    "date": "2026-06-11",
    "notes": "WebP delivery fix: CSS background images (bc_bg_image_style + MediaRenderer slide tracks) now emit a plain url(webp) override with a url(original) fallback instead of image-set()/type(), which -webkit-image-set ignores and Firefox/Safari <=16 reject — so every WebP-capable browser loads the small .webp instead of the multi-MB original.",
    "url": ""
}
